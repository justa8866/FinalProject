import { IconButton } from '@mui/material';
import { styled } from '@mui/material/styles';

export const BoxBar = styled('div')(`
  padding: 1rem; 
  display: inline-block;  

`);

export const IconButtonDesign = styled(IconButton)(`
  font-size: 16px; 
  background: red; 
  color: white

`);
